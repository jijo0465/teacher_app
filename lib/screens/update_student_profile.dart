import 'package:flutter/material.dart';

class UpdateStudentProfile extends StatelessWidget {
  const UpdateStudentProfile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}